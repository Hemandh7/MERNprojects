import React from "react";

const TranscriptComponent = ({ transcript }) => {
  return (
    <div>
      <h2>Transcript</h2>
      <p>{transcript}</p>
    </div>
  );
};

export default TranscriptComponent;
